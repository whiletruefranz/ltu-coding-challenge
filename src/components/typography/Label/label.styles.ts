import { StyleSheet } from 'react-native'
import { ltuBlue } from '../../../constants/colors'

export const LabelStyles = StyleSheet.create({
  label: {
    color: ltuBlue,
    fontFamily: 'PrimaryFont-700',
    fontSize: 16,
    marginBottom: 5,
    marginLeft: 5,
  },
})
